# ---------------------
# GitHub App (required for PR comments/webhooks)
# ---------------------
GITHUB_APP_ID=YOUR_GITHUB_APP_ID
GITHUB_INSTALLATION_ID=YOUR_GITHUB_INSTALLATION_ID
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here
# Path to GitHub App private key (PEM). Example: ./secrets/github-app.pem
GITHUB_PRIVATE_KEY_PATH=./secrets/github-app.pem

# Optional GitHub token for data collection
GITHUB_TOKEN=ghp_your_token_here

# ---------------------
# LLM / AI (optional)
# ---------------------
LLM_PROVIDER=
LLM_MODEL=
# Provider API key (optional) — not tested yet
LLM_API_KEY=
LLM_ENABLED=false
LLM_MAX_TOKENS=500
LLM_TEMPERATURE=0.7
LLM_FALLBACK_TO_BASIC=true

# ---------------------
# Database (optional)
# ---------------------
# Example SQL Server connection string for local Docker instance
# Replace password and server as appropriate. 
Database__ConnectionString=Server=localhost,1433;Database=DeploymentRiskDb;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
# Legacy key used by some scripts
SQL_CONNECTION_STRING=Server=localhost,1433;Database=DeploymentRiskDb;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;
# SA password (used by docker-compose examples)
SQL_SA_PASSWORD=YourStrong!Passw0rd

# ---------------------
# ML service
# ---------------------
ML_SERVICE_BASE_URL=http://localhost:8000
ML_SERVICE_TIMEOUT_SECONDS=30

# ---------------------
# API / Frontend
# ---------------------
API_PORT=5000
FRONTEND_URL=http://localhost:4200

# ---------------------
# JWT (optional)
# ---------------------
# Must be at least 32 chars for cryptographic safety
JWT_SECRET_KEY=replace_with_a_32_char_secret_or_longer
JWT_ISSUER=DeploymentRiskPlatform
JWT_AUDIENCE=DeploymentRiskPlatform

# ---------------------
# Feature flags
# ---------------------
ENABLE_ML_MODEL=true
ENABLE_CODEQL_SCANNING=true
ENABLE_LLM_REVIEWS=false

# ---------------------
# Development settings
# ---------------------
ASPNETCORE_ENVIRONMENT=Development
DOTNET_ENVIRONMENT=Development

# ---------------------
# NOTES
# ---------------------
# - Copy this file to `.env` and populate values locally.
# - Use `GITHUB_PRIVATE_KEY_PATH` to point at a PEM file stored in `./secrets`.